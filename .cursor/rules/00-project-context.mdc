---
description: docker-infra 项目稳定上下文：定位、必读文档、命名约定、AI 协作边界
alwaysApply: true
---

# 项目核心上下文

## 项目定位

`docker-infra` 是一个**学习 + 求职**项目，运行在跨多云厂商的真实服务器上，**项目本身就是简历**。所有代码都必须能被叙事、被讲解、能回答"为什么选 X 而不是 Y"。

求职目标：**初级云运维 / 运维开发 / DevOps 工程师**（深圳/广州）。参考系是"中小型科技公司云运维工程师日常会用到什么"，不参考大厂超大规模架构。

## 必读文档（每次开始新任务前先读）

1. `.cursor/rules/01-user-profile.mdc` — 用户背景、求职定位、决策原则与红线
2. `.cursor/rules/15-current-facts.mdc` — 动态事实查询入口（节点清单、架构语义、凭据等）
3. `Docs/architecture/` 目录下版本号最大的那份 — 当前线上架构语义事实
4. `inventory/hosts.yml` — 当前节点运行时事实（hostname、Ansible 分组、SSH 信息）
5. `Docs/scheme/phase-1-architecture-upgrade.md` — 当前演进路线图

涉及文档撰写时遵循 `.cursor/rules/10-docs-workflow.mdc`。

## 当前集群规模与命名约定

**当前规模概述**（仅作 minimal context；具体节点清单、IP、规格、角色一律以最新架构快照与 `inventory/hosts.yml` 为准）：

- 跨多个云厂商的真实多云集群
- 节点分布于广州与北京两地
- 同城节点之间走 Tailscale 内网延迟较低；跨城节点延迟较高（具体实测值见最新架构快照"网络互联"段）

**节点 hostname 命名约定**：

| 维度 | 约定 |
|------|------|
| 格式 | `<region>-<NN>` |
| `region` | 2 位小写字母城市代码（`gz`、`bj`、`sh`、`hk`、`sg` 等） |
| `NN` | 2 位零填充数字序号（`01`-`99`） |
| 序号分配 | 同一 region 内严格递增；**不复用**已退役节点的序号 |
| role 是否进 hostname | **不进**，hostname 与角色解耦，角色信息只出现在架构快照 |
| 大小写 | 全小写 |
| 例外处理 | 不符合该约定的命名需在架构快照中显式说明原因 |

**节点互联与目录约定**（不随版本变化）：

- 节点互联走 **Tailscale WireGuard** 加密隧道，不依赖公网端口暴露
- Ansible 控制仓库 `/opt/docker-infra` 仅由 Ansible 控制节点持有
- 各节点线上运行目录统一为 `/opt/docker`，由 Ansible 渲染下发，不在该目录执行 git pull/clone

## AI 协作边界

**必须**：
- 输出前先用 1-2 句话解释每段配置/代码的意图，让用户能在面试中复述
- 引入新组件前先回答"为什么选 X 而不是 Y"，理由要能写进 retrospective
- 改动跨多个目录时主动提示需要联动更新哪些文档

**禁止**：
- 堆砌当前用不到的组件（"看起来专业"）
- 为"看起来专业"而无理由扩节点；如方案确实需要额外节点支撑，必须先说明理由再评估采购
- 给出能跑通但讲不清的代码
- 把敏感信息（密码、token、webhook URL 真值）写入任何普通文件
- 修改节点 IP / 配置规格 / 延迟值时跳过流程：必须先更新 `inventory/hosts.yml` 与最新架构快照，规则文件不再硬编码这些动态事实

## 决策红线（来自用户画像）

- 有状态服务（MySQL / Redis / ProxySQL）**不**进 K3s，继续 Compose 承载
- 不引入 Harbor、PagerDuty、ELK 等对当前规模 overkill 的组件
- 不跳过 runbook / retrospective 直接打架构 tag
- 不在没有可叙事理由时引入新技术
- 技术选型深度不够时不急于进入下一个主题

## 输出语言

文档正文与注释使用中文；代码、命令、文件路径、容器名、变量名保持英文原样。
