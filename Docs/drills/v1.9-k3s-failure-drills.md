# 演练：v1.9 K3s 若依发布与节点故障三类演练

> **演练日期**：2026-06-12
> **演练时架构版本**：V1.9（v1.8→v1.9 迁移中，模块二切流已完成、Compose 老副本在线兜底，模块二 review 前执行）
> **演练范围**：在 K3s 接管若依（`ruoyi` namespace，2 副本跨 `ops-lab-gz-04` / `ops-lab-gz-06`）后，验证三类受控故障的"现象 → kubectl 定位 → 恢复"闭环：① 发布不存在的镜像 tag（ImagePullBackOff）；② readinessProbe 配错端口（rollout 卡住）；③ drain worker 节点（Pod 驱逐与重调度）
> **演练等级**：受控演练（**不停业务**——每类演练 `maxUnavailable:0` 与 Compose 老副本共同兜底；演练全程未摘 Compose 老副本）
> **状态**：已完成
> **关联文档**：
> - `Docs/runbooks/v1.8-to-v1.9.md` §Phase 9：本次演练的预案来源
> - `Docs/proposals/k3s-stateless.md` §4：演练验收标准来源
> - `roles/k3s-ruoyi/templates/deployment.yaml.j2`：`strategy`（`maxSurge:1 maxUnavailable:0`）、`topologySpreadConstraints`、`nodeAffinity`（排除 control-plane）、`readinessProbe`（TCP 8080）
> - `Docs/sli-slo.md`：ruoyi 服务可用性目标

---

## 1. 演练目标

本次演练用实测数据回答以下问题：

1. **发布不存在的 tag**：当 `set image` 指向 Registry 中不存在的 tag 时，K8s 是否只替换一个新 Pod 并使其停在 `ImagePullBackOff`、旧副本是否全程在线（`maxUnavailable:0` 生效），`rollout undo` 能否一键恢复？
2. **readinessProbe 配错**：把 readiness 探针指向无监听端口（9999）后，新 Pod 是否会停在 `READY 0/1`、`rollout status` 是否如期超时、旧副本是否因 `maxUnavailable:0` 不被摘除，`rollout undo` 能否恢复？
3. **drain worker 节点**：drain `ops-lab-gz-04` 后其上 Pod 是否被驱逐，替换 Pod 会落到 `ops-lab-gz-06`、保持 `Pending`，还是错误地落到 control-plane `ops-lab-gz-05`？单副本窗口期有多长？`uncordon` 后是否自动恢复跨节点分布？
4. **kubectl 定位能力**：三类故障是否都能用 `get pods` / `describe` / `rollout status` 定位根因？定位命令本身有没有踩坑？

---

## 2. 前置基线

演练开始前的集群状态（演练前若干分钟实测）：

| 项 | 期望状态 | 实测 |
|---|---|---|
| `ruoyi` Deployment 就绪 | `2/2` Running | ✅ `ruoyi 2/2 2 2`（14:21） |
| Pod 跨节点分布 | 分别落 gz-04 / gz-06 | ✅ 两副本分别在 `ops-lab-gz-04` / `ops-lab-gz-06` |
| 当前镜像 | 具体 tag（已脱离 latest） | ✅ `100.118.69.78:5000/ruoyi:15-33303b7`（经 Jenkins `IMAGE_TAG=15-33303b7` 发布）|
| 节点全部 Ready | 3 节点 Ready，无 SchedulingDisabled | ✅ gz-04 / gz-06 worker，gz-05 control-plane |
| Compose 老副本在线 | gz-02 / gz-03 ruoyi 兜底 | ✅ 未摘除（模块二 3 天窗口内）|

---

## 3. 演练步骤

三类演练均在 bj-01 执行（`export KUBECONFIG=~/.kube/k3s-ruoyi.yaml`），目标集群为 K3s `ruoyi` namespace。

### 演练 ① — ImagePullBackOff（发布不存在的 tag）

| 要素 | 内容 |
|---|---|
| ① 步骤目标 | 验证发布不存在镜像时旧副本不受影响、新 Pod 卡在拉取失败、undo 可恢复 |
| ② 节点 | bj-01（操作）/ 集群 ruoyi ns |
| ③ 具体命令 | `kubectl -n ruoyi set image deployment/ruoyi ruoyi=100.118.69.78:5000/ruoyi:nonexistent-tag` → `kubectl -n ruoyi get pods` → `kubectl -n ruoyi describe pod -l app=ruoyi`（定位）→ `kubectl -n ruoyi rollout undo deployment/ruoyi`（恢复）|
| ④ 观察窗口 | `kubectl -n ruoyi get pods` 的 STATUS 列 |
| ⑤ 期望现象 | 新 Pod `ImagePullBackOff`；2 个旧 Pod 始终 Running；undo 后恢复 |
| ⑥ 异常退出条件 | 旧副本被提前摘除导致就绪副本 < 1 → 立即 undo |

### 演练 ② — readinessProbe 配错端口

| 要素 | 内容 |
|---|---|
| ① 步骤目标 | 验证 readiness 失败时新 Pod 不接流量、rollout 卡住、旧副本不被摘、undo 可恢复 |
| ② 节点 | bj-01（操作）/ 集群 ruoyi ns |
| ③ 具体命令 | `kubectl -n ruoyi patch deployment ruoyi --type=json -p='[{"op":"replace","path":"/spec/template/spec/containers/0/readinessProbe/tcpSocket/port","value":9999}]'` → `kubectl -n ruoyi rollout status deployment/ruoyi --timeout=60s` → `kubectl -n ruoyi get pods` → `kubectl -n ruoyi rollout undo deployment/ruoyi` |
| ④ 观察窗口 | `rollout status` 输出 + `get pods` 的 READY 列 |
| ⑤ 期望现象 | `rollout status` 60s 超时；新 Pod `Running` 但 `READY 0/1`；2 旧 Pod 仍 Running；undo 恢复 |
| ⑥ 异常退出条件 | 就绪副本 < 1 → 立即 undo |

### 演练 ③ — drain worker 节点

| 要素 | 内容 |
|---|---|
| ① 步骤目标 | 验证 drain 驱逐 Pod 后的重调度行为与单副本窗口期，确认不落 control-plane |
| ② 节点 | bj-01（操作），目标 worker `ops-lab-gz-04` |
| ③ 具体命令 | `kubectl drain ops-lab-gz-04 --ignore-daemonsets --delete-emptydir-data --timeout=120s` → `kubectl -n ruoyi get pods -o wide` → `kubectl uncordon ops-lab-gz-04` |
| ④ 观察窗口 | `get pods -o wide` 的 NODE / STATUS 列；`kubectl get nodes` 的 SchedulingDisabled |
| ⑤ 期望现象 | gz-04 上 Pod 被驱逐；替换 Pod 落 gz-06 或 `Pending`，**不落 gz-05**；uncordon 后恢复跨节点分布 |
| ⑥ 异常退出条件 | 替换 Pod 调度到 control-plane gz-05 → 立即记录并排查 affinity |

---

## 4. 实测观察

> 时间戳取自 bj-01 终端提示符（当日 UTC+8）。秒级以提示符显示时刻为准。

### 4.1 关键时间轴

| 时刻 | 事件 | 现象 |
|---|---|---|
| 14:36:30 | 演练① `set image` 指向 `:nonexistent-tag` | `deployment.apps/ruoyi image updated` |
| 14:37:01 | 演练① 查 Pod | 新 Pod `ruoyi-5b9674b84-7p2n4 0/1 ImagePullBackOff`；旧 Pod 2 个 `1/1 Running` |
| 14:37:44 | 演练① `rollout undo` | `deployment.apps/ruoyi rolled back` |
| 14:37:52 | 演练① 恢复确认 | 2 副本 `1/1 Running` |
| 14:38:08 | 演练② patch probe → 9999 | `deployment.apps/ruoyi patched` |
| 14:38:40 | 演练② `rollout status --timeout=60s` | `1 out of 2 new replicas have been updated...` 后 `error: timed out waiting for the condition`（约 60s 后） |
| 14:39:54 | 演练② 查 Pod | 新 Pod `ruoyi-6c4b75cb7d-sc927 0/1 Running`；旧 Pod 2 个 `1/1 Running` |
| 14:40:01 | 演练② `rollout undo` | `rolled back` |
| 14:40:14 | 演练② 恢复确认 | 2 副本 `1/1 Running` |
| 14:40:24 | 演练③ `drain gz-04`（短名） | `Error from server (NotFound): nodes "gz-04" not found` |
| 14:40:51 | 演练③ `drain ops-lab-gz-04`（全名） | `cordoned` → `evicting pod ruoyi-7b94c95558-r7vl5` → `evicted` → `drained` |
| 14:42:49 | 演练③ 查 Pod | 替换 Pod `ruoyi-7b94c95558-7vqdh 0/1 Pending`（无节点）；`jzdsm 1/1 Running` 在 gz-06 → **单副本在线** |
| 14:43:03 | 演练③ `uncordon gz-04`（短名） | `NotFound` |
| 14:43:15 | 演练③ `uncordon ops-lab-gz-04`（全名） | `node/ops-lab-gz-04 uncordoned` |
| 14:43:22 | 演练③ 查 Pod | `7vqdh` 调度回 `ops-lab-gz-04`，`0/1 Running`（启动中）；`jzdsm 1/1 Running` 在 gz-06 |
| ~14:45 | 演练③ 收尾确认 | `7vqdh 1/1 Running`；`get nodes` 三节点全 `Ready` 无 SchedulingDisabled；`rollout status` 成功 |

### 4.2 各演练核心结论

| 演练 | 期望 | 实测 | 判定 |
|---|---|---|---|
| ① ImagePullBackOff | 旧副本不受影响、新 Pod 卡拉取、undo 恢复 | 完全符合：新 Pod `ImagePullBackOff`，旧 2 副本全程 Running，undo 秒级恢复 | ✅ 通过 |
| ② readinessProbe | rollout 超时、新 Pod `READY 0/1`、旧副本不摘、undo 恢复 | 完全符合：60s 超时，新 Pod `0/1 Running` 不接流量，旧 2 副本在线，undo 恢复 | ✅ 通过 |
| ③ drain | Pod 驱逐、不落 gz-05、uncordon 恢复 | 符合且暴露单副本窗口：替换 Pod **Pending 而非落 gz-06**（topologySpread `DoNotSchedule` + gz-05 被排除），uncordon 后回到 gz-04 | ✅ 通过（含关键发现）|

### 4.3 用户视角影响

| 指标 | 实测 |
|---|---|
| 演练期间公网 5xx 次数 | **未实测**（本次未挂 `while true; curl https://ruoyi.jjmstart.com` 循环，无公网视角数据）|
| 演练①②期间就绪副本数 | 全程 ≥ 2（`maxUnavailable:0` 保证旧副本不摘）|
| 演练③期间就绪副本数 | 降至 **1**（gz-06 上 `jzdsm`），持续约 14:40:51 → 14:43:22（≈2.5 分钟），直至 uncordon 后替换 Pod 在 gz-04 重新就绪 |
| Compose 老副本兜底 | 全程在线，未摘除 |

---

## 5. 心得 / 反直觉发现

- **`topologySpreadConstraints` 的 `whenUnsatisfiable: DoNotSchedule` 在 drain 单 worker 时会直接牺牲副本数**：drain gz-04 后，替换 Pod 没有像直觉那样"挤"到 gz-06，而是停在 `Pending`——因为 gz-06 已有 1 副本，再调度会让 `maxSkew:1` 不满足，且 gz-05 是 control-plane 被 affinity 排除。结果是 2 worker 架构下任一 worker 维护期间，ruoyi 只能单副本运行。这是 HA 设计的真实边界，不是 bug。
- **`maxUnavailable:0` 在两类发布故障里表现一致且可靠**：演练①②中旧副本全程在线，坏镜像 / 坏探针都只影响"新 Pod"，老流量零中断，印证了 `maxUnavailable:0 + maxSurge:1` 的"先起新、再撤旧"语义。
- **K8s 节点名是 `ops-lab-gz-04`，不是 inventory 里的 `gz-04`**：drain / uncordon 各踩了一次 `NotFound`。kubectl 操作节点前应先 `kubectl get nodes` 确认真实 node name，inventory 的 Ansible 主机名与 K8s node name 是两套命名。
- **`describe pod ... | grep -A5 -iE 'Events|Failed|Back-off'` 没能捞出根因**：演练①里这条定位命令返回空，`describe` 的事件区格式没匹配到该 grep。直接 `kubectl -n ruoyi describe pod <pod>` 看 Events 段、或 `kubectl -n ruoyi get events --sort-by=.lastTimestamp` 更可靠。

---

## 6. 待改进项

| 类别 | 内容 |
|---|---|
| 恢复步骤需要补 runbook | runbook §Phase 9 用的是短节点名 `gz-04`，实际 K8s node name 为 `ops-lab-gz-04`，drain / uncordon 会 `NotFound`。建议把 Phase 9 命令改为真实 node name，或加一步 `kubectl get nodes` 前置确认 |
| 定位命令需要修正 | runbook 里 `describe pod -l app=ruoyi | grep -A5 -iE 'Events|Failed|Back-off'` 实测捞不到根因，建议改为 `kubectl -n ruoyi get events --sort-by=.lastTimestamp` 或直接 describe 指定 Pod |
| 下次演练改进 | 本次未挂公网 `curl` 循环，缺用户视角 5xx / 延迟数据。下次演练③（drain）务必同时跑 `while true; do curl -s -o /dev/null -w "%{http_code} " https://ruoyi.jjmstart.com/; sleep 1; done`，量化单副本窗口期对公网的实际影响 |
| 新发现的盲点 | 2 worker + `DoNotSchedule` 拓扑下，单 worker 维护即降为单副本。是否需要：①第 3 个 worker；②维护窗口临时把 `whenUnsatisfiable` 放宽为 `ScheduleAnyway`；③接受单副本窗口并写入 SLO。建议评估后决定是否新开 review |
| 是否回填到其他文档 | drain 单副本窗口（≈2.5 分钟）与 `Docs/sli-slo.md` ruoyi 可用性目标的关系需复核 |

---

## 7. 演练自我评价

- **本次演练验证了什么核心假设**：K3s 接管若依后，`maxUnavailable:0 + maxSurge:1` 的滚动发布在坏镜像、坏探针两类故障下都能保证旧副本零中断，`rollout undo` 是可靠的一键回滚手段。
- **本次演练否定了什么预设**：原以为 drain 一个 worker 后副本会自动迁到另一 worker 维持 2 副本；实测因 `topologySpreadConstraints: DoNotSchedule` + control-plane 排除，副本会停在 `Pending`，HA 在 2 worker 架构下 drain 期间只剩单副本。
- **如果这是真实故障，当前架构的最大短板是什么**：worker 数量只有 2 个且强制打散，任一 worker 不可用即无冗余；control-plane gz-05 被刻意排除业务，不参与兜底。
- **可沉淀为后续 review 引用的 3 条工程结论**：①`DoNotSchedule` 是"宁可少副本也不违反分布"的硬约束，与"维护期间保副本数"目标冲突，需按场景取舍；②`maxUnavailable:0` 让发布类故障的爆炸半径恒等于"新 Pod"，与节点类故障的可用性影响要分开评估；③K8s node name 与 Ansible inventory 主机名是两套命名，跨系统操作前必须以 `kubectl get nodes` 为准。
