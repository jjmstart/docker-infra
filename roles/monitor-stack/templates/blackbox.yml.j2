modules:
  # HTTP 探测用于公网入口和管理端健康检查；允许 3xx 是为了兼容 HTTPS/登录页跳转。
  http_2xx:
    prober: http
    timeout: 5s
    http:
      valid_status_codes: [200, 301, 302]
      method: GET
      preferred_ip_protocol: ip4

  # TCP 探测预留给后续只需验证端口连通性的服务，不校验应用层响应内容。
  tcp_connect:
    prober: tcp
    timeout: 5s