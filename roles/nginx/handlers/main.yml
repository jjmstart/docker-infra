---
- name: reload nginx
  community.docker.docker_container_exec:
    container: nginx
    command: nginx -s reload

- name: restart nginx
  community.docker.docker_compose_v2:
    project_src: "{{ docker_root }}/proxy/nginx"
    state: present
    recreate: auto
